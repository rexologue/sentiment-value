model_path: /app/model
prefer_cuda: true
enable_compile: false
cluster_model_path: null  # укажите путь к kmeans.joblib, чтобы включить /cluster
cluster_pca_path: null    # путь к PCA (опционально)
cluster_label_map_path: null  # YAML/JSON с маппингом cluster_id -> class_id (0,1,2)
host: 0.0.0.0
port: 8000

csv_batch_size: 64

log_level: INFO
log_format: plain
